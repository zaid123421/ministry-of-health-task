# React Project – Installation & Run Guide

هذا الملف يشرح خطوات **تثبيت وتشغيل مشروع React** بشكل واضح ومرتب.

---

## 📋 المتطلبات (Prerequisites)

قبل البدء، تأكد من توفر الأدوات التالية على جهازك:

* **Node.js**: الإصدار **22 LTS** أو **20.19+**
* **Package Manager**: أحد الخيارات التالية:

  * `pnpm` (مفضل)
  * `npm`
  * `yarn`

للتحقق من الإصدارات:

```bash
node -v
pnpm -v
```

---

## 📦 تثبيت المشروع (Installation)

### 1️⃣ نسخ المشروع (Clone Repository)

```bash
git clone <repository-url>
cd <project-folder>
```

### 2️⃣ تثبيت الحزم (Install Dependencies)

#### باستخدام pnpm (مفضل):

```bash
pnpm install
```

#### أو باستخدام npm:

```bash
npm install
```

---

## ▶️ تشغيل المشروع (Run Project)

### وضع التطوير (Development Mode)

```bash
pnpm dev
```

أو:

```bash
npm run dev
```

بعد التشغيل، افتح المتصفح على:

```
http://localhost:5173
```

(قد يختلف المنفذ حسب الإعدادات)

---

## 🛠️ أوامر مفيدة (Useful Scripts)

| الأمر          | الوصف                      |
| -------------- | -------------------------- |
| `pnpm dev`     | تشغيل المشروع بوضع التطوير |
| `pnpm build`   | إنشاء نسخة الإنتاج         |
| `pnpm preview` | معاينة نسخة الإنتاج        |

---

## ⚠️ ملاحظات مهمة

* **إصدارات Node الفردية (مثل 21)** غير مدعومة وقد تسبب أخطاء.
* في حال حدوث مشاكل:

  1. تأكد من إصدار Node.
  2. احذف `node_modules` وملف lock ثم أعد التثبيت.

```bash
rmdir /s /q node_modules
del pnpm-lock.yaml
pnpm install
```

---

## 📁 التقنيات المستخدمة (Tech Stack)

* React
* Vite
* JavaScript / TypeScript
* pnpm

---

## ✅ جاهز للتشغيل

بعد تنفيذ الخطوات السابقة، سيكون المشروع جاهزاً للعمل 🎉

بالتوفيق 🚀
